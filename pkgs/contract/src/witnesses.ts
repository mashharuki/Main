// Patient Registry用のプライベート状態の型定義
// 現在はプライベート状態を持たないため、空のオブジェクト
export type PatientRegistryPrivateState = Record<string, never>;

// Witness関数の実装
// 現在のコントラクトではwitness関数を使用していないため、空のオブジェクト
export const witnesses = {};
